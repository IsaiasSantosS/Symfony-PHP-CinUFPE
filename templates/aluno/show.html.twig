{% extends 'base.html.twig' %}

{% block title %}Aluno{% endblock %}

{% block body %}
    <h1>Aluno</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aluno.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ aluno.nome }}</td>
            </tr>
            <tr>
                <th>Cidade</th>
                <td>{{ aluno.cidade }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('aluno_index') }}" class="btn btn-dark">back to list</a>

    <a href="{{ path('aluno_edit', {'id': aluno.id}) }}" class="btn btn-info">edit</a>

    {{ include('aluno/_delete_form.html.twig') }}
{% endblock %}
